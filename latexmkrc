$ENV{'TZ'}='America/Monterrey';

# Configuración mejorada para SyncTeX con archivos R Sweave
# $pdflatex = 'pdflatex -synctex=1 -interaction=nonstopmode -file-line-error %O %S';
# $pdf_mode = 1;
# $out_dir = '';

# # Para archivos .Rtex, usar el archivo .tex generado
# @default_files = ('*.tex');